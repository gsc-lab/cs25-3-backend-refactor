# 📋 체크리스트
- [ ] 코드가 정상적으로 동작하나요? (로컬 또는 Docker 환경에서 테스트 완료)
- [ ] main이 아닌 develop/feature 브랜치에서 작업했나요?
- [ ] 브랜치 이름이 규칙에 맞나요? (예: feature/be-create-student-api)
- [ ] 커밋 메시지가 의미 있고 일관성 있게 작성되었나요?
- [ ] 기존 API나 기능에 영향을 주지 않나요?
- [ ] `.env`, `vendor/` 등, 민감 정보가 커밋되지 않았나요?

---

# ✅ 변경 사항 요약
> 이번 PR에서 구현하거나 수정한 내용을 요약해주세요.

**예시**
- StudentsController에 CRUD API 추가 (`GET/POST/PUT/DELETE /students`)
- AltoRouter 기반 라우트 등록 (`router.php`)
- DB 연결용 `.env` 예시 추가 (`.env.example`)
- CORS 설정 추가 (`public/index.php`)

---

# 🧩 상세 변경 내용
> 파일 단위로 주요 변경 사항을 정리해주세요.

**예시**
- `app/Controllers/StudentsController.php` : CRUD 메서드 구현  
- `router.php` : `/students` 관련 라우트 추가  
- `config/database.php` : PDO 연결 에러 핸들링 개선  
- `composer.json` : 의존성 업데이트 (`ext-pdo`, `vlucas/phpdotenv`)  
- `.gitignore` : `.env`, `vendor/` 제외 확인

---